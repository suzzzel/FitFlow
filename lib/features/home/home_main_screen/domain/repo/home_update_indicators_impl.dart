abstract class HomeUpdateIndicatorsDomainImpl {
  Future<bool> updateAge({required int newAge});
  Future<bool> updateHeight({required int newHeight});
  Future<bool> updateWeight({required int newWeight});
}
